[package]
name = "cavacore-sys"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Raw FFI bindings to cavacore, the audio-visualization engine from CAVA"
links = "cavacore"

[build-dependencies]
cc = "1"

[dependencies]
